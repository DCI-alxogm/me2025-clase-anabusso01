#include <stdio.h>
#include <math.h>

int main(){
  float a, b, c;
  float D, x1, x2, real, imag;
  char repetir;

  do{
    printf("=== Raíces de ax^2+bx+c=0===\n");
    printf("Introduce a: ");
    scanf("%f", &a);
    printf("Introduce b:");
    scanf("%f", &b);
    printf("Introduce c:");
    scanf("%f", %c);

    if (a==0) {
      if (b==0){
        if (c==0){
          printf("infinitas soluciones (identidad 0=0)\n");
        } else {
          printf("Sin solución")
        }
        }
      }
    }
  }
}